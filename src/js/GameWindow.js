class GameWindow {
    constructor(height, width, ctx) {
        this.height = height;
        this.width = width;
        this.context = ctx;
    }
    renderObject(object) { }
}
export { GameWindow };
